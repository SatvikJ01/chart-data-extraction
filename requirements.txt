# Core inference
torch>=2.0
torchvision>=0.15
transformers>=4.30
datasets>=2.12
numpy>=1.24
pandas>=2.0
Pillow>=9.5

# Marker-detection preprocessing (preserved from the original notebook:
# cv2 grayscale read + albumentations resize, distinct from the axis stage's
# PIL/torchvision path -- the two checkpoints were trained differently).
opencv-python>=4.7
albumentations>=1.3

# Tests
pytest>=7.0
